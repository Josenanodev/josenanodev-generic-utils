/**
 * Validates if a string is a valid SQL date format
 * @param dateString string to validate
 * @returns a boolean
 */
declare function isSQLDateFormat(dateString: string): boolean;
export default isSQLDateFormat;
